<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Items</title>
    <link rel="stylesheet" href="/public/main.css">
    <!-- Bootstrap & jQuery CDN -->
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <!-- Latest compiled and minified CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Latest compiled JavaScript -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script> 
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p"
        crossorigin="anonymous"></script>
</head>

<body>
    <div class="container mt-4">
        <div class="d-flex justify-content-center mb-3">
            <h2>Items</h2>
        </div>

        <% if (message) { %>
            <div class="alert alert-warning">
                <%= message %>
            </div>
            <% } %>

                <% if (items.length> 0) { %>
                    <div class="table-responsive">
                        <table class="table table-hover">
                            <thead>
                                <tr>
                                    <th>ID</th>
                                    <th>Title</th>
                                    <th>Post Date</th>
                                    <th>Category Id</th>
                                    <th>Published</th>
                                    <th class="price-column">Price</th>
                                    <th></th>
                                </tr>
                            </thead>
                            <tbody>
                                <% items.forEach(item=> { %>
                                    <tr>
                                        <td>
                                            <%= item.id %>
                                        </td>
                                        <td>
                                            <%= item.title %>
                                        </td>
                                        <td>
                                            <%= new Date(item.postDate).toISOString().split('T')[0] %>
                                        </td>
                                        <td>
                                            <%= item.category %>
                                        </td>
                                        <td>
                                            <%= item.published ? 'Yes' : 'No' %>
                                        </td>
                                        <td class="price-column">$<%= item.price.toFixed(2) %>
                                        </td>
                                        <td class="text-end">
                                            <a href="/items/delete/<%= item.id %>" class="btn btn-danger btn-sm"
                                                onclick="return confirm('Are you sure you want to delete this item?');">
                                                remove
                                            </a>
                                        </td>
                                    </tr>
                                    <% }); %>
                            </tbody>
                        </table>
                    </div>
                    <% } else { %>
                        <div class="alert alert-info">No items found. <a href="/items/add" class="alert-link">Add your
                                first post</a></div>
                        <% } %>
    </div>

    <div class="d-flex justify-content-center mb-3">
        <a href="/items/add" class="btn btn-success position-relative">Add Item</a>
    </div>

    <script>
        document.querySelectorAll('.nav-link').forEach(link => {
            if (link.href === window.location.href) {
                link.classList.add('active');
                link.setAttribute('aria-current', 'page');
            }
        });
    </script>
</body>
</html>